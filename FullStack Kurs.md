**Modul 1 – Web-Grundlagen für FullStack-Entwickler*** *Die Benutzeroberfläche spricht mit dem Backend.** *Das Backend spricht mit der Datenbank.** *Die Benutzeroberfläche spricht nicht direkt mit der Datenbank.** *HTTP ist das Protokoll für die Kommunikation zwischen Client und Server.** *Eine Anfrage geht von der Benutzeroberfläche an das Backend.** *Eine Antwort kommt vom Backend zurück.** *GET liest Daten.** *POST legt neue Daten an.** *PUT ändert vorhandene Daten vollständig.** *PATCH ändert vorhandene Daten teilweise.** *DELETE löscht Daten.** *CRUD bedeutet Create, Read, Update, Delete.** *JSON ist ein textbasiertes Datenformat zum Austausch strukturierter Daten.** *REST baut Schnittstellen ressourcenorientiert auf.** *Saubere Endpunkte sind zum Beispiel /api/rechnungen und /api/rechnungen/5.** *404 Not Found bedeutet: Die angeforderte Ressource wurde nicht gefunden.** *CORS ist eine Browser-Sicherheitsprüfung zwischen Benutzeroberfläche und Backend.** *Fachlogik wie Berechnungen gehört ins Backend.** *Die Datenbank speichert Daten dauerhaft, aber sie ist nicht das Backend.*1\. Warum spricht das Frontend nicht direkt mit der Datenbank?* Das Frontend nimmt nur die Eingaben des Nutzers an und gibt sie weiter ans Backend.* Das Frontend ist nur zuständig für die Präsentationslogik, es geht hier um die Abbildung des Userverhaltens.* Das Backend nimmt die Nutzer-Eingaben über das Frontend entgegen und validiert die Eingaben. Dabei werden die Eingaben in die Geschäftslogik eingearbeitet.* Das Backend stell Sicherheitsprüfungen, Berechnungen und baut die Fachlichkeit der Anwendung auf. Die Daten werden ab hier dann in die Datenbank geschrieben.2\. Was ist der Unterschied zwischen Frontend, Backend und Datenbank?* Frontend bildet die Präsentationslogik ab bzw. übersetzt das Userverwalten auf der Benutzeroberfläche.* Das Backend bildet die Fachlichkeit der Anwendung ab. Stellt Plausibilitäten bereit und gibt dem Frontend wichtige Infos zur Verfügung. Von der Fachlogik werden die Eingaben in die Datenbank geschrieben.* In der Datenbank können CRUD-Befehle ausgeführt werden.* Das Hertzstück der Anwendung ist lediglich das Backend. Dieser bestimmt wo es lang geht!3\. Wofür steht CRUD?* C-Create* R-Read* U-Update* D-Delete* das sind Datenbankbefehle. Durch diese Befehle können Daten in der Datenbank manipuliert werden. Sowohl anlegen von Datensätzen, bis zu auslesen der Daten, Ändern der Daten und löschen von Daten aus der Datenbank.4\. Welche HTTP-Methode nutzt du, um eine neue Rechnung anzulegen?* Mit der POST-Methode können Daten in die Datenbank angelegt werden5\. Welche HTTP-Methode nutzt du, um alle Rechnungen zu laden?* Die GET-Methode liefert das auslesen der Daten6\. Was bedeutet Statuscode 404?* 404 Not Found -> Ressource nicht gefunden7\. Was ist JSON?* JSON ist eine Dateiformat, mit dem Frontend und Backend kommunizieren, eine Art Geheimcode zwischen den beiden Schichten* Die Schlüsselparameter müssen eindeutig sein und müssen vom beiden Schichten gleich Aufgebaut sein, damit die Daten ausgetauscht werden können!8\. Warum kann CORS lokal ein Thema werden?* CORS ist eine Sicherheitsüberprüfung von Browser.* Wenn Client und Server auf unterschiedlichen Ports laufen, kann keine Request-Abfrage vom Browser nicht freigegeben werden.* Die Einstellung muss evtl. in Spring Boot vorgenommen werden. Ich glaube man muss die IP-Adresse bzw. wenn die Anwendung auf einem externen Server läuft muss auf der Backend-Seite gewisse Freigaben erteilt werden.Kleine PraxisaufgabeOrdne die passenden HTTP-Methoden zu:Alle Rechnungen laden → GET /api/invoicesEine Rechnung mit ID 5 laden → GET /api/invoices/id/5Neue Rechnung anlegen → POST /api/invoicesRechnung mit ID 5 komplett ändern → PUT /api/invoices/id/5Rechnung mit ID 5 löschen → DELETE /api/invoices/id/5**Modul 2 – React-Grundlagen und erstes Frontend-Gerüst*** *React baut Benutzeroberflächen aus Komponenten auf.** *Eine Komponente ist eine Funktion, die Benutzeroberfläche zurückgibt.** *In React denkst du in kleinen wiederverwendbaren Bausteinen.** *JSX ist eine HTML-ähnliche Schreibweise in React.** *In JSX heißt class immer className.** *Werte und Ausdrücke werden in JSX mit { } eingebettet.** *Komponenten beginnen mit einem Großbuchstaben.** *Props sind Eingabewerte für Komponenten.** *Props kommen von außen in eine Komponente hinein.** *State sind veränderliche Daten innerhalb einer Komponente.** *Props und State sind nicht dasselbe.** *TypeScript-Typen wie Rechnung geben deinen Daten Struktur.** *Komponenten sollen klare Aufgaben haben.** *Große Dateien werden in kleinere Komponenten aufgeteilt.** *Daten fließen in React von oben nach unten.** *Anwendung.tsx gibt Daten an RechnungsListe.tsx weiter.** *Listen werden oft mit map() gerendert.** *Jedes Element in einer Liste braucht in React meist einen eindeutigen key.** *Eine saubere Struktur macht React-Code lesbarer und wartbarer.** *Erst die Struktur sauber verstehen, dann Interaktivität und Schnittstellen anbinden.*1. Was ist eine React-Komponente?   * React ist an sich eine Bibliothek für Benutzeroberflächen   * Und die Benutzeroberflächen werden in Komponenten aufgebaut diese sind React-Komponenten   * Eine Komponente hier ist eine Funktion die die Benutzeroberfläche zurückgibt2. Was ist der Unterschied zwischen Props und State?   * Props sind Eingabewerte für Komponenten bzw. sowas wie ein PLatzhalter, damit in der Obersten-Komponente der Wert gesetzt werden kann - hier z.B. Titel wird in Kopfbereich eingegeben oder festgelegt durch die Komponente Anwendung!   * Durch Props werden Komponenten wiederverwendbar - Props kommen von außen   * State dahingegen werden innerhalb der Komponente behandelt   * Soweit ich es verstanden habe, wurde kein State verwendet in Rechnungs-Anwendung oder?3. Warum nutzen wir TypeScript-Typen wie Rechnung?   * TypeScript-Typen helfen uns eine klare Struktur zu definieren   * Unsere Entitäten können wir hier sauber abbilden und die variablen sauber definieren4. Warum braucht map() in React meist einen key?   * das weiß ich nicht. Du musst mir das genauer erklären5. Warum haben wir RechnungsListe ausgelagert und nicht alles in Anwendung.tsx geschrieben?   * Die Komponente RechnungsListe kümmert sich nur ums anzeigen der Daten. Die Daten werden in Anwendung.tsx gefüllt bzw. bereitgestellt.   * Die Daten von oben nach unten weitergeben   * Jede Komponente hat eine Aufgabe zu erfüllen und daher sollte man nicht alles in der Hauptkomponente abbilden. Das wäre unübersichtlich und keine Struktur würde das Programmieren erschweren!**Modul 3 – State, Eingaben, Ereignisse und erste Interaktivitaet*** *useState speichert veränderliche Werte in einer Komponente.** *onChange reagiert auf Änderungen in Eingabefeldern.** *Ein gesteuertes Eingabefeld bekommt seinen Wert aus dem State.** *Abgeleitete Daten wie gefilterteRechnungen werden berechnet, nicht extra gespeichert.** *State liegt dort, wo die Logik gesteuert wird.*1. Was ist useState?   * Durch den useState können veränderliche Werte gespeichert werden.   * es ist ein React-hook   * diese Art von speichern vergleiche ich mit der Collection Funktion bei Java.   * der Zustand des Benutzerverhaltens muss irgendwo gespeichert werden da bietet React diese Lösung.   * Vorteil davon ist es, dass auf die Veränderungen des Benutzers sehr schnell reagiert werden kann.   * Dieser Hook ist eigentlich eine Art von Programmierstil oder eine sehr gute Herangehensweise bei einer typtypisierte sprache welches nicht objekt orientiert ist bietet es einen großer Vorteil an den Code zu Clustern!2. Warum brauchen wir fuer das Suchfeld State?   * Das Suchfeld bietet eine Veränderung an also dynamisches Benutzerverhalten!   * Daher müssen wir hier die Veränderung festhalten und sofort wieder zurückgeben!3. Was macht onChange?   * die onChange Method übersetzt das Benutzerverhalten und erzeugt ein Ereignis.   * Dieses Verhalten sehe genau so wie in Java, wenn man in MVC-Pattern Prinzip programmiert, da hat man in Control die Möglichkeit das Userverhalten direkt anzunehmen, zu übersetzen und an die nächsten Stellen weiterleiten, so verstehe ich es!4. Warum speichern wir gefilterteRechnungen nicht extra im State?   * Unter gefilterteRechnungen speichern wir mehrere Veränderungen ab.   * das heißt im Objekt Rechnung ändern wir hier zwei Variablen.   * Diese zwei Variablen wurden bereits in States gesetzt und unter gefilterteRechnungen nehme die direkte Änderungen an und schreibe diese in unsere Liste bzw. Filtern nur noch den Veränderungen!5. Was ist ein gesteuertes Eingabefeld?   * das heißt für mich die Änderung durch Userverhalten wird in Echtzeit übersetz und sofort angezeigt!   * Der State reguliert hier alles.   * am Anfang ist ein Standardwert festgelegt hier ein leer String und durch Eingabe ins Feld wird die Änderung sofort sichtbar und der State aktualisiert sich mit!6. Warum liegt der State in Anwendung.tsx und nicht in RechnungsListe.tsx?   * In der Anwendung.tsx werden die Daten bereitgestellt und deshalb wird auch die Präsentationslogik hier aufgebaut.   * die restlichen Komponenten sind nur für die Anzeige zuständig - klare Trennung der Aufgaben!   * Ich sehe das wie folgt: Anwendung.tsx beinhaltet für mich das Model und Control   * RechnungsListe.tsx ist für das View zuständig! MVC-Prinzip Denkweise!**Modul 4 – Formulare, neue Rechnungen anlegen und Daten nach oben melden*** *Die wichtigsten Sätze aus Modul 4 für deine Notizen** *Formulare bestehen aus mehreren gesteuerten Eingabefeldern.** *Veränderliche Formularwerte werden im State gespeichert.** *Eine Unterkomponente kann über eine Callback-Prop Daten nach oben melden.** *Eine Callback-Prop ist eine Funktion, die als Prop übergeben wird.** *preventDefault() verhindert das Neuladen der Seite beim Absenden eines Formulars.** *Die Hauptliste der Rechnungen liegt in Anwendung.tsx, weil dort die zentrale Steuerung stattfindet.** *RechnungsFormular.tsx verwaltet die Eingaben, RechnungsListe.tsx zeigt Daten nur an.** *Wenn neuer State vom alten State abhängt, nutzt man die Funktionsschreibweise im Setter.** *Anzeige und Logik sollen in React sauber getrennt bleiben.** *Neue Rechnungen werden oben im Hauptzustand ergänzt und danach automatisch neu angezeigt.*1. Was ist eine Callback-Prop?   * Eine Callback-Prop ist dafür da, dass die Hauptkomponente nicht überlagert wird.   * Es wird Sinngemäß eine neue Unterkomponente erstellt. In diesem Fall Rechnungsformular.   * In Rechnungsformular wird der Anwendungsfall Rechnung anlegen behandelt.   * Und in der Hauptkomponente wird dieser nur aufgerufen.   * Sowas betrachte ich als Java Entwickler in einem Trichter-Modell2. Warum liegt die Rechnungs-Liste in Anwendung.tsx?   * Anwendung.tsx ist hier die Hauptkomponente und es werden alle Daten die Angezeigt werden sollen hier gebündelt und weitergegeben!   * Wahrscheinlich wird hier irgendwann auch der API-Aufruf behandelt.3. Warum hat das Formular eigenen State?   * Alle veränderlichen Daten brauchen einen State!   * Da wir eine Unterkomponente bestimmt haben ist es auch sinnvoll diesen State in Rechnungsformular abzubilden!4. Warum verwenden wir preventDefault()?   * Damit die Seite nicht Neu geladen wird!5. Warum nutzen wir beim Aktualisieren der Rechnungen die Funktionsschreibweise im Setter?   * weil der Setter den State zur Änderung zwingt bzw. hier:     * const \[rechnungen, setzeRechnungen] = useState<Rechnung\[]>     * setzeRechnungen den State ändern kann und Rechnungen nur den aktuellen Stand wiedergibt!6. Warum ist RechnungsListe.tsx weiter nur für die Anzeige zuständig?   * Weil wir in Komponenten Prinzip arbeiten   * jeder Anwendungsfall wird in Komponenten definiert und alles was zur Anzeige oder Eingabe oder Ausgabe zuständig ist, bekommt eine Komponente   * Die RechnungsListe.tsx hat nur die Aufgabe die Liste anzuzeigen und dieser wird üblicherweise in der Hauptkomponente aufgerufen!**Modul 5 – Komponenten sauber schneiden, Wiederverwendung und Vorbereitung auf Backend-Anbindung*** *Eine gute Komponente hat eine klare Verantwortung.** *Anwendung.tsx soll die Anwendung steuern und zusammensetzen, aber nicht jede Einzelaufgabe selbst enthalten.** *Datenzugriff sollte nicht direkt in jeder Komponente stehen.** *rechnungsDienst.ts kapselt den Datenzugriff.** *Der Dienst simuliert aktuell Daten, wird später aber echte API-Aufrufe an das Backend enthalten.** *Die Komponente soll nicht wissen, woher die Daten genau kommen.** *useState speichert Zustand.** *useEffect führt Seiteneffekte aus, zum Beispiel das Laden von Daten.** *useEffect wird hier genutzt, um Startdaten beim ersten Laden der Komponente zu laden.** *FilterBereich.tsx ist eine eigene Komponente, weil Filteranzeige und Filtereingabe eine klare Aufgabe sind.** *Gemeinsame Typen wie NeueRechnungDaten gehören an einen zentralen Ort.** *Ladezustand und Fehlerzustand liegen in Anwendung.tsx, weil dort die zentrale Steuerung der Datenanzeige stattfindet.** *Abgeleitete Daten wie gefilterteRechnungen werden weiterhin berechnet, nicht extra gespeichert.** *Die Trennung von Darstellung, Steuerung und Datenzugriff macht den Code wartbarer.** *Diese Struktur bereitet die spätere Backend-Anbindung sauber vor.** *Später kann rechnungsDienst.ts von Mock-Daten auf echte HTTP-Aufrufe umgestellt werden.** *Eine gute Struktur reduziert spätere Umbauten.** *Nicht alles, was man auslagern kann, sollte man sofort auslagern.** *Ausgelagert wird dann, wenn Logik wiederverwendet wird oder eine Datei zu groß und unübersichtlich wird.** *In größeren Projekten werden häufig Dienste, eigene Hooks, Hilfsfunktionen, Typen und Komponenten getrennt organisiert.**Für den jetzigen Stand ist unsere Struktur genau richtig:**Anwendung.tsx**├─ steuert die Anwendung**├─ hält Hauptzustände**├─ lädt Daten**├─ entscheidet über Lade-, Fehler- und Anzeigezustände**└─ verbindet Formular, Filter und Liste**Die Unterkomponenten bleiben klar spezialisiert:**RechnungsFormular.tsx -> Eingabe neuer Rechnungen**FilterBereich.tsx     -> Suche und Statusfilter**RechnungsListe.tsx    -> Anzeige der Rechnungen**rechnungsDienst.ts    -> Daten laden**Rechnung.ts           -> gemeinsame Typen*1. Warum haben wir einen rechnungsDienst.ts eingeführt?   * in dem dienst werden die Datenaufrufe simuliert.   * das ist eine Vorbereitung für den API-Aufruf   * Eine saubere Trennung von Service aufrufen die dann in Anwendung.tsx gesteuert wird2. Was macht useEffect in diesem Modul?   * Zu dem Zeitpunkt des Ladens des Services wird hier bestimmt   * das heißt wir bestimmen zu welchem Zeitpunkt die Daten geladen werden.   * Dadurch hat man im Frontend die Kontrolle über die API-Aufrufe   * Vorher waren die Daten in useState geladen und jetzt wird diese Logik ist useEffect übergeben bzw. eine Erweiterung von useState3. Warum ist FilterBereich.tsx als eigene Komponente sinnvoll?   * Eine gute Erweiterung der Komponentengliederung   * Anwendung.tsx steuert und bündelt alle Komponenten aber hier ist es sinnvoll die Aufgabenbereiche in weitere Komponenten zu gliedern4. Warum liegen Ladezustand und Fehlerzustand in Anwendung.tsx?   * Weil hier die Steuerung in Anwendung.tsx haben   * Gewisse Bereiche können sinngemäß ausgelagert werden in Komponenten aber die Steuerung und Kontrolle hat Anwendung.tsx quasi das Gehirn der Frontend-Logik!5. Warum ist NeueRechnungDaten jetzt zentral in Rechnung.ts definiert?   * Es ist sehr sinnvoll dies in Rechnung.ts auszulagern, weil es mehrere Aufrufe in verschiedenen Komponenten geben kann   * Das bildet direkt schon einen Anwendungsfall ab, man könnte zum Beispiel auch RechnugnsDatenAendern erweitern oder RechnugnsDatenLoeschen.   * All diese Fälle können sauber in den jeweiligen Typen abgebildet werden und Unterstützt die Wartbarkeit!6. Warum ist diese Struktur besser für die spätere Backend-Anbindung?   * Wir haben alles auf einen API-Aufruf vorbereitet.   * In rechnungsDienst könnte man den Backend-Aufruf einbinden - hier soll der Dienst nicht wissen woher und wie die Daten kommen sondern nur das Liefern was ihm aufgetragen wird   * Wir haben alle Aufgabenbereiche in Komponenten gegliedert, alles sauber definiert, die Anwendungsfälle vorbereitet für einen API-Aufruf.   * Meiner Meinung nach könnte man Anwendung.tsx die Steuerung ebenfalls weiter auslagern z.B. den  useEffect(() … Aufruf oder function rechnungAnlegen(daten: NeueRechnungDaten) etc, diese könnte man doch weiter unter Steuerung oder so auslagern und diese Dann gebündelt in Anwendung.tsx aufrufen - was meinst du hier, ich würde gerne deine Meinung dazu wissen und erzähl mir bitte wie es in der Praxis aussieht also in echte großen Projekten...?**Modul 6 – Erste Backend-Anbindung mit Spring Boot und fetch***Ein wichtiger Architekturpunkt für die nächsten Module:** *Frontend-Dienst      -> ruft Backend per HTTP auf** *Backend-Controller   -> nimmt HTTP-Anfrage entgegen** *Backend-Dienst       -> enthält Fachlogik** *Repository           -> kommt später und spricht mit PostgreSQL** *Datenbank            -> speichert dauerhaft** *Das Frontend ruft das Backend mit fetch auf.** *fetch sendet HTTP-Anfragen vom Browser an einen Backend-Endpunkt.** *RechnungsController.java stellt REST-Endpunkte bereit.** *RechnungsDienst.java enthält die fachliche Logik und liefert Daten für den Controller.** *@RestController kennzeichnet eine Klasse als REST-Controller.** *@RequestMapping("/api/rechnungen") legt den Basis-Pfad für die Endpunkte fest.** *@GetMapping verbindet eine Java-Methode mit einer HTTP-GET-Anfrage.** *@PathVariable liest Werte aus dem URL-Pfad, zum Beispiel die ID aus /api/rechnungen/1.** *@CrossOrigin erlaubt dem Browser, vom React-Frontend aus auf das Backend zuzugreifen.** *CORS ist eine Browser-Sicherheitsregel.** *Backend und Frontend laufen lokal auf unterschiedlichen Ports.** *Das Frontend läuft typischerweise auf localhost:5173.** *Das Backend läuft typischerweise auf localhost:8080.** *Beide Anwendungen müssen gleichzeitig laufen.** *antwort.ok prüft, ob die HTTP-Antwort erfolgreich war.** *Spring Boot wandelt Java-Objekte automatisch in JSON um.** *Die Feldnamen im Backend müssen zu den Feldnamen im Frontend passen.** *Der erste echte FullStack-Ablauf ist: React → fetch → Spring Controller → Dienst → JSON → React State → Anzeige.** *Später wird der Backend-Dienst nicht mehr Mock-Daten liefern, sondern über Repository und PostgreSQL echte Daten laden.** *Der Controller soll nicht direkt mit der Datenbank sprechen.*1. Was ist die Aufgabe von RechnungsController.java?   * Ganz einfach gesagt dieser Controller stellt die Rest-Endpunkte bereit.   * Rechnungsdienst wird hier aufgerufen, das heißt indirekt könnte ein Select-Befehl von Postgres aufgerufen werden vom Controller.   * Hier sieht man die klassische Komponenten Aufteilung in der Objektorientierten Programmierung   * Controller stellt die Endpunkte und Dienst ruft das Datenbank-Befehl2. Was ist die Aufgabe von RechnungsDienst.java?   * Wie in 1 beschrieben, RechnungsDienst stellt nur die Daten bereit bzw. ruft den Datenbank-Befehl auf.   * In unserem Beispiel arbeiten wir mit Mock-Daten und füllen unsere Rechnungsliste statisch   * Sowohl alle Rechnungen als auch Rechnungen nach der Id werden für den Controller bereitgestellt3. Warum nutzen wir @GetMapping?   * Die Definition eines Endpunktes!   * Da wir vorher schon im Requestmapping die URL definiert haben, greift GetMapping auf diesen Pfad und stellt die durch die Methode bereitgestellte Liste in diesem Fall   * Also der Aufruf von RechnungsDienst wird veröffentlicht für den Frontend4. Warum brauchen wir @CrossOrigin?   * Hier wird definiert welcher Frontend auf das Backend zugreifen darf!   * Also 8080 und 5173 dürfen miteinander kommunizieren5. Was macht fetch im Frontend?   * Fetch sorgt für die Kommunikation zwischen Frontend und Backend   * Hier wird eine HTTP-Request Anfrage an das Backend gesendet und das Backend schaut sich im CrossOrigin ob es der freigegebene Port zugreifen darf!6. Warum prüfen wir antwort.ok?   * hier prüfen auf antwort.ok, wenn die Anfrage fehlerhaft oder nicht erreichbar sein sollte und das die Anwendung nicht zu einem Abbruch führen soll7. Was passiert Ende-zu-Ende, wenn React ladeRechnungen() aufruft?   * Hier passiert folgendes:     * Frontend lädt die Rechnungsliste zum ersten mal Asynchron     * Also das Objekt Rechnung bzw. Rechnungsliste wird gefüllt.     * Durch den Backendaufruf fetch(API\_BASIS\_URL) erhalten wir hier eine Antwort, wenn das Backend nicht erreichbar ist, dann wird eine Fehlermeldung ausgegeben. Hier wird eine HTTP-Request Anfrage an das Backend gestellt. Das Backend lässt die Anfrage zu, wenn es durch CORS der Frontend Port freigegeben ist, dann erst können sie miteinander erfolgreich kommunizieren.     * Im Backend ruft der Controller den Dienst auf. Die API URL wird durch den Controller für die außen Welt freigegeben bzw. bereitgestellt!     * Letzten Endes Konvertiert das Backend die Daten in JSON, sodass das Frontend damit weiterarbeiten kann!     * Nicht zu vergessen, wenn Backend und Frontend in diesem Fall erfolgreich kommuniziert haben, dann wird die Rechnungsliste gefüllt und in RechnungsListe.tsx die Daten angezeigt.**Modul 7 – Daten vom Frontend ans Backend senden*** *POST wird verwendet, um neue Daten anzulegen.** *POST /api/rechnungen legt eine neue Rechnung an.** *Das Frontend sendet Daten im HTTP-Body an das Backend.** *JSON.stringify(daten) wandelt Frontend-Daten in JSON-Text um.** *@RequestBody wandelt den JSON-Body im Backend in ein Java-Objekt um.** *NeueRechnungAnfrage ist das Request-Modell für neue Rechnungen.** *Das Backend erzeugt die ID.** *Das Backend berechnet Steuer und Brutto.** *Fachlogik gehört ins Backend.** *Nach erfolgreichem Anlegen gibt das Backend 201 Created zurück.** *Das Backend gibt die neu angelegte Rechnung als JSON zurück.** *Das Frontend wartet mit async/await auf die Backend-Antwort.** *Nach erfolgreicher Antwort ergänzt React die Rechnung im State.** *Die Liste aktualisiert sich automatisch durch den neuen State.** *Daten im Arbeitsspeicher gehen bei einem Backend-Neustart verloren.** *Dauerhafte Speicherung kommt später mit PostgreSQL.*1. Warum nutzen wir für das Anlegen einer Rechnung POST?   * Post steht als Funktion für Create, das heißt neue Daten können angelegt werden!   * Post ist ein REST-Endpunkt die über httpRequest kommuniziert wird zwischen frontend und backend2. Was macht @RequestBody?   * RequestBody wandelt die Variablen in dem neuen Objekt NeueRechnungAnfrage zu JSON-Objekt um.   * Das heißt wenn von außen ist nur JSON Objekte zulässig für diese Post-Methode   * Die Daten werden in dem Body vom Frontend zum Backend gesendet3. Warum gibt das Backend nach dem Anlegen 201 Created zurück?   * In der Post-Methode in Spring setzen wir den Status bei erfolgreicher Anlage 201 und das bedeutet der Datensatz wurde erfolgreich angelegt4. Warum berechnet jetzt das Backend Steuer und Brutto?   * Die Fachlogik wird generell im Backend berechnet und Frontend hat die Aufgabe die Daten anzuzeigen/weiterleiten5. Was macht JSON.stringify(daten)?   * Konvertiert die Daten von NeueRechnungDaten ins JSON-Format damit das Body in Backend vollständig gefüllt ist.6. Warum ist rechnungAnlegen im Frontend jetzt async?   * Das ist hier die Besonderheit von Reac! hier muss das Frontend nicht auf 201 Created warten kann trotzdem die GUI Aufbauen und erneut rendern sobald das Backend aufgerufen wird7. Warum ist die neu angelegte Rechnung nach einem Backend-Neustart wieder weg?   * weil wir aktuell nur in den Speicher des Backends verwenden und keine Datenbank aufgesetzt haben**Modul 8 – Validierung, Fehlerbehandlung und saubere Rückmeldungen*** ***Frontend-Validierung hilft dem Benutzer.**** ***Backend-Validierung schützt die Anwendung.**** ***Das Backend muss Eingaben immer selbst prüfen.**** ***@Valid startet die Validierung eines Request-Objekts.**** ***@RequestBody wandelt JSON in ein Java-Objekt um.**** ***@NotBlank verhindert leere Texte und reine Leerzeichen.**** ***@NotNull verhindert fehlende Werte.**** ***@DecimalMin prüft einen Mindestwert.**** ***@Size begrenzt die Länge eines Feldes.**** ***400 Bad Request bedeutet: Die Anfrage ist ungültig.**** ***409 Conflict bedeutet: Die Anfrage steht im Konflikt mit dem aktuellen Zustand.**** ***Doppelte Rechnungsnummern sind ein fachlicher Konflikt.**** ***@RestControllerAdvice organisiert Fehlerbehandlung zentral.**** ***Eine zentrale Fehlerbehandlung vermeidet doppelte Fehlerlogik in Controllern.**** ***Fehlerantworten sollten strukturiert als JSON zurückgegeben werden.**** ***Das Frontend sollte Backend-Fehler auslesen und verständlich anzeigen.**** ***alert() ist für professionelle Formulare ungeeignet.**** ***Formularfehler sollten direkt im Formular angezeigt werden.**** ***Saubere Fehlerbehandlung macht eine Anwendung robuster und benutzerfreundlicher.***1. Warum reicht Frontend-Validierung alleine nicht aus?   * Die Backend-Validierung hilft dabei die Anwendung zu schützen und ist Pflicht!   * Frontend Validierung hilft den Benutzer2. Was macht @Valid?   * Prüft das Request Objekt bei rechnungAnlegen anhand der Validierungsregeln   * Die Validierungsregeln sind diese Annotationen   * @NotBlank   * @NotNull   * @DecimalMin   * @Size3. Was macht @NotBlank?   * Die Variable im Backend mit dieser Annotation darf nicht leer sein.4. Was bedeutet 400 Bad Request?   * Bedeutet das die Anfrage ungültig ist5. Was bedeutet 409 Conflict?   * Die Anfrage hat zwar funktioniert aber weist auf einen Konflikt zu den man im Backend abgefangen hat6. Warum verwenden wir eine zentrale Fehlerbehandlung mit @RestControllerAdvice?   * ist eine Hilfreiche Annotation, dadurch müssen wir nicht in allen Controller-Methoden die Fehler einzeln behandeln   * Zentrale Organisation von Fehlerbehandlung!7. Warum ersetzen wir alert() durch eine Fehleranzeige im Formular?   * Der Fehler vom Backend wird hier ausgelesen bzw. wird als JSON-Objekt ins Frontend weitergeleitet und im Frontend wird dieser dann übersetzt8. Was passiert Ende-zu-Ende, wenn eine doppelte Rechnungsnummer gesendet wird?   * Das Backend lässt doppelte REchnungsnummern nicht zu und wirft einen CONFLICT (409) vor   * Dadurch wird der Meldungstext vom Backend über JSON-Obejekt ins Frontend übertragen und dann im Formular angezeigt!**Modul 9 – PostgreSQL und echte Speicherung*** ***PostgreSQL speichert unsere Daten dauerhaft.**** ***Daten im Arbeitsspeicher gehen beim Neustart der Anwendung verloren.**** ***Daten in PostgreSQL bleiben erhalten, solange der Datenbankspeicher nicht gelöscht wird.**** ***Eine JPA-Entity ist eine Java-Klasse, die eine Datenbanktabelle abbildet.**** ***@Entity kennzeichnet eine Klasse als Datenbank-Entity.**** ***@Table legt fest, zu welcher Tabelle die Entity gehört.**** ***@Id kennzeichnet den Primärschlüssel.**** ***@GeneratedValue sorgt dafür, dass die Datenbank IDs erzeugt.**** ***Ein Repository ist die Datenzugriffsschicht.**** ***JpaRepository stellt Standardmethoden wie findAll, findById, save und deleteById bereit.**** ***Spring Data JPA kann aus Methodennamen automatisch Abfragen erzeugen.**** ***existsByRechnungsNummerIgnoreCase prüft, ob eine Rechnungsnummer bereits existiert.**** ***Der Controller spricht nicht direkt mit dem Repository.**** ***Die saubere Schichtenfolge ist: Controller → Dienst → Repository → Datenbank.**** ***spring.jpa.hibernate.ddl-auto=update erstellt oder aktualisiert Tabellen anhand der Entities.**** ***Für Lernprojekte ist ddl-auto=update praktisch.**** ***In echten Produktivsystemen nutzt man für Datenbankänderungen eher Migrationen.**** ***Docker Compose startet PostgreSQL reproduzierbar als Container.**** ***Das Docker-Volume postgres\_daten speichert die PostgreSQL-Daten unabhängig vom Container.**** ***Mit PostgreSQL hat unsere Anwendung jetzt echte persistente Speicherung.**** ***Eine Bean ist ein Objekt, das von Spring erzeugt und verwaltet wird.**** ***Der Spring-Container heißt ApplicationContext.**** ***Dependency Injection bedeutet, dass Abhängigkeiten von außen bereitgestellt werden.**** ***Spring erzeugt Beans und verbindet sie automatisch.**** ***@Component, @Service, @Repository und @RestController machen Klassen zu Spring-Komponenten.**** ***@Service steht für Fachlogik.**** ***@RestController steht für REST-Endpunkte.**** ***@Component ist eine allgemeine Spring-Komponente.**** ***Konstruktor-Injection ist in modernen Spring-Projekten die bevorzugte Variante.**** ***Bei einem einzigen Konstruktor braucht man in Spring meistens kein @Autowired.**** ***@Inject aus CDI und Spring Dependency Injection folgen demselben Grundprinzip.**** ***Spring Data JPA erzeugt Repository-Implementierungen automatisch.**** ***Der Controller erzeugt den Dienst nicht selbst mit new.**** ***Der Dienst erzeugt das Repository nicht selbst mit new.**** ***Spring übernimmt die Verkabelung der Klassen.**** ***Dadurch wird der Code besser testbar, wartbarer und loser gekoppelt.**** ***Dependency Injection bedeutet: Abhängigkeiten werden von außen bereitgestellt, statt innerhalb der Klasse selbst erzeugt.***  * ***Spring: @Autowired oder Konstruktor-Injection**** ***Vorteile:***  * ***besser testbar***  * ***weniger Kopplung***  * ***austauschbare Implementierungen***  * ***klarere Verantwortlichkeiten***  * ***Spring kann Lebenszyklus verwalten******@Component     -> allgemeine Spring-Komponente******@Service       -> Fachlogik / Geschäftslogik******@Repository    -> Datenzugriff******@RestController -> REST-Endpunkte / HTTP-Schicht******RechnungsController.java******-> @RestController******-> HTTP-Anfragen entgegennehmen******RechnungsDienst.java******-> @Service******-> Fachlogik ausführen******RechnungsRepository.java******-> Repository-Bean******-> Datenbankzugriff******StartDatenLader.java******-> @Component******-> technische Startlogik******------------------------------------------------------------------******Wie Spring unsere Klassen verbindet******Spring startet***&#x20; ***|***&#x20; ***v******Spring scannt Package de.rechnungsanwendung.backend***&#x20; ***|***&#x20; ***v******Spring findet @RestController RechnungsController***&#x20; ***|***&#x20; ***v******Spring sieht: Controller braucht RechnungsDienst***&#x20; ***|***&#x20; ***v******Spring findet @Service RechnungsDienst***&#x20; ***|***&#x20; ***v******Spring sieht: Dienst braucht RechnungsRepository***&#x20; ***|***&#x20; ***v******Spring erzeugt Repository-Bean automatisch über Spring Data JPA***&#x20; ***|***&#x20; ***v******Spring gibt Repository in Dienst***&#x20; ***|***&#x20; ***v******Spring gibt Dienst in Controller******---------------------------------------------------------------------******Also entsteht diese Kette:******RechnungsController***&#x20;       ***braucht******RechnungsDienst***&#x20;       ***braucht******RechnungsRepository***&#x20;       ***spricht mit******PostgreSQL******Das ist Dependency Injection in unserer echten Anwendung.******----------------------------------------------------------------------******Spring erzeugt Beans und injiziert ihre Abhängigkeiten über den Konstruktor. Dadurch sind die Klassen lose gekoppelt, besser testbar und klarer strukturiert.***1. ***Was ist eine Bean?***   1. ***Antwort:***      1. ***Eine Bean ist ein von Spring erzeugtes und verwaltetes Objekt im ApplicationContext.***2. ***Was ist Dependency Injection?***   1. ***Antwort:***      1. ***Dependency Injection bedeutet, dass eine Klasse ihre benötigten Abhängigkeiten nicht selbst erzeugt, sondern von außen durch den Container bekommt.***3. ***Warum Konstruktor-Injection?***   1. ***Antwort:***      1. ***Weil Pflichtabhängigkeiten klar sichtbar sind, final verwendet werden kann, Tests einfacher sind und das Objekt nach der Erstellung vollständig initialisiert ist.***4. ***Unterschied @Autowired und @Inject?***   1. ***Antwort:***      1. ***Beide dienen der Injektion von Abhängigkeiten. @Inject kommt aus dem Jakarta-/CDI-Standard, @Autowired aus Spring. In modernen Spring-Projekten wird häufig Konstruktor-Injection ohne explizite Annotation verwendet.***5. ***Warum nicht überall new?***   1. ***Antwort:***      1. ***Weil Spring den Objektlebenszyklus verwaltet, Abhängigkeiten automatisch verbindet und dadurch lose Kopplung und bessere Testbarkeit ermöglicht.***6. ***Kann eine Spring-Klasse mehrere Dependencies haben?***   1. ***antworten:***      1. ***Ja. Eine Spring Bean kann über Konstruktor-Injection mehrere Abhängigkeiten erhalten. Spring sucht im ApplicationContext für jeden Konstruktorparameter eine passende Bean und injiziert sie automatisch. Problematisch wird es nur, wenn es mehrere Beans desselben Typs gibt, dann braucht man zum Beispiel @Qualifier oder @Primary. Wenn eine Klasse sehr viele Dependencies hat, ist das oft ein Hinweis auf zu viele Verantwortlichkeiten.******-------------------------------------------------------------------------------------------------------------******mehrere verschiedene Beans -> kein Problem******mehrere Beans vom gleichen Typ -> @Qualifier oder @Primary nötig******zu viele Dependencies -> mögliches Architekturproblem******zirkuläre Dependencies -> meistens schlechtes Design******-----------------------------------------------------------------------------------------------------------***1. Warum brauchen wir jetzt PostgreSQL?   1. wir brauchen es um Daten in eine Datenbank speichern zu können.   2. wir bauen eine Verbindung zwischen SpringBoot und PostgreSQL über Docker.   3. Im Docker starten wir Datenbank und bauen eine Verbindung zwischen Backend und Datenbank auf   4. Wir wollen nicht mehr die Daten im Backenspeicher speichern da sie bei jedem Neustart der Anwendung verschwinden. Deshalb Postgres DB!2. Was ist der Unterschied zwischen Arbeitsspeicher und Datenbank?   1. Im Arbeitsspeicher und DB können Daten gespeichert werden.   2. Im Arbeitsspeichern sind die neu gespeicherten Daten nur solange gültig bis die Anwendung (Backend) läuft   3. im DB können wir die Daten für immer festhalten   4. DB ist die letzte Schicht im Entwicklungslandschaft!3. Was ist eine JPA-Entity?   1. Eine JPA-Entitiy ist eine JAVA-Klasse   2. Sie hat den Vorteil eine Tabelle in die DB anzulegen   3. Das heißt eine Entity sagt wie ein Objekt in DB gespeichert werden sollen bzw. welche Parameter etc!4. Was ist ein Repository?   1. In unserem Beispiel nutzen wir die JpaRepository in unserem Interface RechnungsRepository   2. Hier haben wir zusätzlich existsByRechnungsNummerIgnoreCase implementiert ohne eine Implementierungsmethode, das verstehe ich bis jetzt nicht!   3. Aber durch den Interface RechnungsRepository können in RechnungDienst die Standardmethoden von CRUD nutzen (save, findAll etc)5. Warum spricht der Controller nicht direkt mit dem Repository?   1. Der Controller hat nur die Aufgabe nach außen die HTTP-Rest Apis zur Verfügung zu stellen!   2. RechnungDienst ist in Verbindung zu Repository und REpository mit der Datenbank!   3. Klare OOP !6. Was macht spring.jpa.hibernate.ddl-auto=update?   1. bei jedem Start den Anwendung werden die Enttiys als DB Tabellen angelegt wenn sie nicht existieren!7. Warum bleiben Daten nach einem Backend-Neustart jetzt erhalten?   1. Ganz einfach, weil die neuen Daten jetzt in DB gesepichert werden und die Verbindung zwischen Backend und DB aufgebaut wurde!   2. Wir greifen ab jetzt in die DB-Tabellen durch unseren Backend!8. Welche Rolle spielt das Docker-Volume postgres\_daten?   1. Um einen Container in Docker zu erstellen sollte ein Volumen existieren   2. EIn Volume kann soweit ich weiß mehrere Container beinhalten und dem volumes werden glaube ich Speicherplatz automatisch zugewiesen!